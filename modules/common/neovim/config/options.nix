# Neovim options
# Use :options to get the list of all options
# Use :h <option> to load help for given <option>
{
    opts = {
        # copy indent from previous line
        copyindent = true;

        # highlight current line
        cursorline = true;

        # show substitution preview in split window
        inccommand = "split";

        # infer casing on word completion
        infercase = true;

        # show line numbers
        number = true;

        # preserve indentation as much as possible
        preserveindent = true;

        # display line numbers relative to current line
        relativenumber = true;

        # number of spaces to use for indentation by default
        shiftwidth = 4;

        # use smart indentation
        smartindent = true;
    };
}
